#!/bin/bash

# Kimi wrapper script - uses pass for secure API key storage
export ANTHROPIC_BASE_URL=https://api.moonshot.ai/anthropic
export ANTHROPIC_AUTH_TOKEN=$(pass show llm/kimi_api_key 2>/dev/null | head -1)

# Use the absolute path to claude
exec /Users/smian/.claude/local/claude "$@"