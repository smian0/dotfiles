# Borg repository configuration
# Repository location (absolute path)
REPO_PATH="$HOME/.dotfiles-backups/borg-repo"
# Passphrase source (use pass store)
PASSPHRASE_CMD="pass show dotfiles/borg-passphrase"
